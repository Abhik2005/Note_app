<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link
href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet"
/>
</head>
<body>
    <div class="min-w-full min-h-screen bg-zinc-900">
        <h1 class="text-4xl text-gray-200 font-bold capitalize text-center mb-5">
            Create your own todo(Note)
        </h1>
        <div class="w-full p-10">
            <form action="/create" method="post">
                <input  class="block w-full outline-none rounded-md p-2" placeholder="Enter title of Todo"  type="text" name="title">
                <textarea class="block w-full outline-none mt-3 resize-none rounded-md p-2" placeholder="Enter description of your Todo..." name="details"></textarea>
                <input type="submit" class="block px-3 py-2 bg-blue-600 rounded-md mt-2">
            </form>
        </div>
        <div class="w-full px-10 flex flex-wrap gap-3">
            <%if(todoTitle.length>0){%>
                <% todoTitle.forEach((todo)=>{%>
                    <div class="bg-zinc-600 w-[fit-content] p-2 rounded-md relative">
                       <a href="/todo/delete/<%= todo %>"><i class="close ri-close-line absolute top-0 right-1"></i></a>
                <h1 class="text-gray-600 text-2xl font-semibold tracking-tighter"><%= todo %></h1>
               <h1 class="text-blue-500"><a href="/todo/<%= todo %>">Read more...</a></h1>
                    </div>
                    <%})%>
                <%}else{%>
                    <h3 class="text-xl text-gray-400 font-semibold tracking-tighter">No todos here yet.</h3>
                    
<% } %>
    
    </div>
  </div>

    <script src="/javascripts/script.js"></script>
</body>
</html>